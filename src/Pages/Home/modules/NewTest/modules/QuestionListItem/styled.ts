import styled from 'styled-components';

export const BoxWithEllipsis = styled.div`
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
  flex: 1;
  margin-left: 12px;
  margin-right: 15px;
`;
